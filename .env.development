# Defaults de desenvolvimento (`pnpm dev`). Sem secrets.
# Secrets ficam em `.env.local` (gitignored).
# Produção: variáveis na Vercel (Turso de prod), não neste arquivo.

NEXT_PUBLIC_DATA_SOURCE="http"

# SQLite local — isolado da produção
DATABASE_URL="file:local.db"
